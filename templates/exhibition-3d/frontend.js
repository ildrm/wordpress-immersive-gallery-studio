(()=>{'use strict';function init(g){if(g.dataset.igsTemplateReady)return;g.dataset.igsTemplateReady='1';const stage=g.querySelector('.igs-stage');if(!stage)return;let down=false,x=0,rot=0,moved=0;stage.style.transformStyle='preserve-3d';stage.addEventListener('pointerdown',e=>{down=true;x=e.clientX;moved=0;stage.setPointerCapture(e.pointerId)});stage.addEventListener('pointermove',e=>{if(!down)return;const dx=e.clientX-x;moved+=Math.abs(dx);rot+=dx*.03;x=e.clientX;stage.style.transform=`rotateX(4deg) rotateY(${rot}deg)`});stage.addEventListener('pointerup',()=>{down=false;if(moved>8){g.dataset.dragged='1';window.IGS?.track(g,'interaction');setTimeout(()=>delete g.dataset.dragged,80)}})}const boot=()=>document.querySelectorAll('.igs-template-museum3d').forEach(init);if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot,{once:true});else boot()})();
